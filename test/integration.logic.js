const crypto = require('crypto');

const Storage = artifacts.require("Storage");
const IntegrationLogic = artifacts.require("Logic");

contract('dymmy', accounts => {
  it('dummy ', async() => {
    //todo write some integration tests infolving Logic contacts consuming Storage contract here
    assert(true);
  });
});
